ts: 2026-08-23T23:07:41Z
commit: none (repo has zero commits at time of writing; `git log` errors with "does not have any commits yet")
session: Claude Code session d038135c-38c1-4c34-801a-b73544749faf, controller mutation test after impl-task7 reported DONE
status: verified
fact: A no-op / identity gate instantiated at the DEGENERATE value of the parameter it is meant to protect tests nothing about that parameter. `tests/test_mapper.py::test_self_mapper_is_a_noop_through_inject` was the designated gate for fit-vs-inference feature-layout agreement in the KV mapper, but it selects k=1 source layers — with one layer, layer ORDER is meaningless. Reversing the inference-time layer order in `apply_mapper`'s `feats()` helper (making the fit-time and inference-time feature layouts disagree, the module's worst silent-corruption mode) left ALL FOUR mapper tests passing. The implementer had self-reported "no concerns". Fixed by adding a k=3 variant of the same gate; both a layer-order flip and a head-order flip then fail it.
basis: Mutation applied to `kvt/mapper.py` (`layers = list(layers)[::-1]` inserted into `apply_mapper.feats`), then `.venv/Scripts/python.exe -m pytest tests/test_mapper.py -q` printed "....  [100%]  4 passed in 0.75s" — i.e. the corruption was undetected. After adding `test_self_mapper_is_a_noop_at_k_gt_1_pinning_feature_order`, the same mutation printed "FAILED tests/test_mapper.py::test_self_mapper_is_a_noop_at_k_gt_1_pinning_feature_order / 1 failed, 4 passed", and a head-order flip (`.flip(2)` before reshape) printed "2 failed, 3 passed". `kvt/mapper.py` restored byte-identical after each (md5 4a4acd87e29c71324116d907ebcd88e1, confirmed independently by the fix agent as sole writer).
re-verify: sh -c 'grep -n "select_top_k(r2_sel, 1)\|select_top_k(r2_sel, 3)" tests/test_mapper.py'
