ts: 2026-08-24T21:35:00Z
commit: none at time of writing (working tree; baseline 0487bd6)
session: Claude Code session 26832175, follow-on build, final whole-branch review
status: verified
fact: Two verifications used repeatedly during this build were structurally incapable of reporting failure, and both produced confident false statements before anyone noticed. (1) `cmd 2>&1 | tail -N` run as a background job reports **tail's** exit code, not cmd's. The WP3 perplexity sweep died immediately on a dataset-id error and the harness recorded "exit code 0"; the controller announced the run as started and only discovered the failure when checking for output files that did not exist. Pipe to a file and check the command's own status, or use `set -o pipefail`, whenever an exit code is going to be believed. (2) `git status --short mappers/` was used as evidence that fitted-mapper artifacts were unchanged, but `mappers/` is listed in `.gitignore`, so that command can never print anything about it -- the check was vacuous from the first invocation and was repeated across several turns as though it were evidence. A gitignored path cannot be monitored with git; compare hashes or mtimes instead. The unifying rule: before trusting a check, ask what output it would produce if the thing it guards were broken. If the answer is "the same output", it is not a check. This is the same failure mode this repository exists to study, applied to its own verification layer rather than to a model's reconstruction metric.
basis: (1) Reproduced directly: the background task for `scripts/eval_perplexity.py ... 2>&1 | tail -30` is recorded by the harness as "completed (exit code 0)" while its captured output ends in `huggingface_hub.errors.HfUriError: Invalid HF URI 'hf://datasets/wikitext@...'. Repository id must be 'namespace/name', got 'wikitext'.` and `results/perplexity/` was never created (`ls` returned nothing). Confirmed separately that `Salesforce/wikitext` loads 4358 rows where bare `wikitext` raises. (2) `git check-ignore -v mappers/qwen3-0.6b-to-1.7b/rope/k1.safetensors` returns `.gitignore:7:mappers/`, proving the path is ignored; `git status --short mappers/` prints nothing whether the directory is pristine, modified, or absent. Found by the final whole-branch reviewer, which also noted the related live finding that `git status --short results/` had by then become non-empty (`?? results/mapper/qwen3-0.6b-to-1.7b/rope/`) while the controller was still asserting it was clean.
re-verify: cd ~/dev/kv-transfer-replication && git check-ignore -v mappers/ ; bash -c 'false | tail -1; echo "exit code seen by caller: $?  (0 means the pipe masked the failure)"'
