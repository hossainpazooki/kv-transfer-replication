ts: 2026-08-23T23:55:40Z
commit: none (repo has zero commits at time of writing; `git log` errors with "does not have any commits yet")
session: Claude Code session d038135c-38c1-4c34-801a-b73544749faf, controller evaluation of probe results against pre-registered H1
status: refuted-assumption
fact: The pre-registered prediction "RoPE-stripped keys will fit better than RoPE-contaminated keys" is FALSE on a matched-KV pair whose two models share a rope base and see identical positions. Measured on Qwen3-0.6B -> 1.7B, diagonal-mean held-out probe R^2 is 0.6284 for stripped K against 0.6606 for un-stripped K — stripping made the fit slightly WORSE, not better (at the single best cell it wins by a trivial +0.0038). The reason is that when both sides carry the same rotation, that rotation is a shared orthogonal factor and removing it removes no confound. This does not contradict Heo et al. 2026, whose own ablation finds the coupled-RoPE variant within noise on short context; their content-space mapping is claimed to buy LENGTH GENERALIZATION, which short-context R^2 cannot test either way. The over-sharp prediction was the replicator's, not the paper's. Carry-forward: do not treat "strip the positional encoding" as a fit-quality improvement; its payoff, if any, is position-free reuse across context lengths, and testing it requires varying context length.
basis: `.venv/Scripts/python.exe` over `results/probe/qwen3-0.6b-to-1.7b/r2_*_heldout.npy` printed "stripped=0.6284 rope=0.6606 V=0.4361 | stripped>rope? False". Same probe run, 50 seqs x 1024 tokens, stride 4, probe p/n = 0.0125 so the train/held-out gap is small (0.029-0.042) and the comparison is not confounded by overfitting.
re-verify: .venv/Scripts/python.exe -c "import numpy as np; p='results/probe/qwen3-0.6b-to-1.7b/'; d=lambda k: float(np.mean(np.diag(np.load(p+'r2_'+k+'_heldout.npy')))); print(round(d('K_stripped'),4), round(d('K_rope'),4), d('K_stripped')>d('K_rope'))"
