ts: 2026-08-24T20:47:00Z
commit: none at time of writing (working tree; baseline 0487bd6)
session: Claude Code session 26832175, follow-on build, validating the 420-sequence dump
status: verified
fact: KV dumps produced by `scripts/dump_kv.py` are NOT bit-reproducible across different torch thread counts, and `meta.json` does not record the thread count, so a `re-verify:` line cannot reproduce a dump bit-for-bit without out-of-band knowledge. Identical input tokens and identical weights, run at `--threads 12` versus the default (10 on this box), give values that differ by up to ONE fp16 ULP at the largest magnitudes: layer 0 agrees to 5.96e-08 max abs (its raw fp16 bytes are bit-identical) but the divergence compounds with depth to 3.125e-02 max abs at layer 27 of Qwen3-0.6B, which is exactly one fp16 ULP there; mean abs difference 7.1e-06, relative 3.1e-04. Cause is BLAS reduction order changing with thread count, then fp16 storage rounding the result to an adjacent representable value. CONSEQUENCE: numbers computed from two different dumps of the SAME tokens are comparable to about 3e-04 relative, not exactly. A learning curve or any within-experiment comparison must therefore recompute EVERY point from ONE dump rather than reusing a figure computed from an earlier dump -- otherwise a difference of order 1e-04 between two conditions could be pure thread-count artifact. Do not cross-compare the follow-on 420-sequence curve's n=50 point against the committed Run 2 figures (0.6814 etc.); recompute it.
basis: Controller measurement, in-process, comparing `data/kv/qwen3-0.6b-to-1.7b/source` (dumped with default threads) against sequences 0-49 of `data/kv/qwen3-0.6b-to-1.7b-n420/source` (dumped with `--threads 12`). Token nesting was verified FIRST and is exact -- `np.array_equal(n50_tokens, n420_tokens[:50])` is True -- so the inputs are identical and only the computation differs. Per-layer max abs / relative / mean differences: V layer 0 5.960e-08 / 2.60e-08 / 9.1e-15; V layer 13 1.562e-02 / 4.14e-04 / 1.1e-06; V layer 27 3.125e-02 / 3.13e-04 / 7.1e-06; K_rope layer 0 2.441e-04 / 4.83e-07 / 3.8e-11; K_rope layer 27 3.125e-02 / 6.23e-04 / 8.2e-07. Raw fp16 on disk at layer 0, row 0, dims 0-5 is byte-identical between the two dumps, confirming same tokens and same weights.
re-verify: .venv/Scripts/python.exe -c "import torch; from kvt.data import KVDump; o=KVDump.load('data/kv/qwen3-0.6b-to-1.7b/source'); d=KVDump.load('data/kv/qwen3-0.6b-to-1.7b-n420/source'); m=d.seq_range_mask(0,50); a=o.get('V',27); b=d.get('V',27)[m]; print('exact', torch.equal(a,b), 'maxabs %.3e' % (a-b).abs().max().item(), 'mean %.3e' % (a-b).abs().mean().item())"
