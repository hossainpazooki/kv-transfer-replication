ts: 2026-08-23T23:32:10Z
commit: none (repo has zero commits at time of writing; `git log` errors with "does not have any commits yet")
session: Claude Code session d038135c-38c1-4c34-801a-b73544749faf, controller reproduction of rev-task8b's finding
status: verified
fact: Closing a fail-open by enumerating the known-bad inputs reliably leaves the DEGENERATE case behind, because the degenerate case satisfies every guard vacuously. `scripts/summarize_hellaswag.py` was hardened to raise on a missing results file and on mismatched record counts. An EMPTY `native.jsonl` still slipped through all of it: a file exists (so the no-files guard passes), the `native` key exists (so the missing-native guard passes), all counts agree at zero (so the count guard passes), and the idx sets match as empty sets (so the like-for-like guard passes). The summarizer then emitted `{'n': 0, 'acc': nan, 'acc_norm': nan, 'ci95': [nan, nan], 'retention_raw': nan, 'retention_floor_norm': nan}` with no exception — a NaN row written to a results table, which reads as data. Corollary worth carrying: after hardening any fail-open, explicitly test the zero/empty input, not just the malformed one.
basis: `.venv/Scripts/python.exe` reproduction with `native.jsonl` and `mapped-k1.jsonl` both present and zero-length printed "load_and_validate_records -> NO ERROR, returned: {'mapped-k1': 0, 'native': 0}" then "summarize_records -> NO ERROR. native row: {'n': 0, 'acc': nan, 'acc_norm': nan, 'ci95': [nan, nan], 'retention_raw': nan, 'retention_floor_norm': nan}" (two numpy RuntimeWarnings, no exception). After the second fix pass the same input raises: "ValueError: condition(s) in <dir> produced zero scored examples: ['mapped-k1', 'native']; a run that scored nothing must not be summarized as a valid empty result".
re-verify: .venv/Scripts/python.exe -m pytest tests/test_hellaswag.py -q
