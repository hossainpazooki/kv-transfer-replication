ts: 2026-08-24T20:15:00Z
commit: none at time of writing (working tree; baseline 0487bd6, follow-on build in progress)
session: Claude Code session 26832175, follow-on build (cache economics WP1-WP3), Wave A review
status: verified
fact: Complete-code implementation plans put arithmetic into test ASSERTIONS, and that arithmetic is the one part of the plan nobody executes before an implementer transcribes it. Two of nine tasks in a single plan shipped with a self-inconsistent fixture, both found only at execution time by the implementing agent. (a) Task 4: a record `logprobs=[-1.0,-2.0], nbytes=[10,1], gold=1` was asserted correct under length normalization, but the normalized score is `[-0.1,-2.0]`, argmax 0 != gold 1, so the assertion contradicted the very code the plan also specified. (b) Task 5: a markdown-table row filter `[l for l in lines if l.startswith("| ")][2:]` assumed the separator row `|---|---|` matches the `"| "` prefix; it does not (no space after the leading pipe), so the filter yields header+data only and `[2:]` silently discarded a real data row. The dangerous case is (b): the tempting repair is to change the PRODUCTION format so the buggy test passes, which would have rewritten a committed results artifact. Treat every hand-computed literal in a plan as unrun code: execute the arithmetic while writing the plan, or write the fixture so the expected value is derived rather than typed.
basis: Both recomputed by the controller independently of the implementing agents, in-process. (a) `s=[-1.0/10,-2.0/1]` -> `[-0.1,-2.0]`, `s.index(max(s))==0`, gold 1, so False against an asserted True; the implementer's `nbytes=[1,10]` gives `[-1.0,-0.2]`, argmax 1 == gold -> True, and preserves the test's intent that raw and normalized DISAGREE on that example (raw argmax is 0). (b) `render_table` over 3 conditions emits 5 lines; `[l for l in lines if l.startswith("| ")]` returns 4 (header + 3 data), so `[2:]` gives 2 rows against 3 conditions while `[1:]` gives 3. Confirmed the committed `results/hellaswag/qwen3-0.6b-to-1.7b/summary.md` reproduces byte-identically under the accepted fix (`git status --short results/` empty; native 0.598 / mapped-k1 0.548 / mapped-k8 0.312 unchanged).
re-verify: .venv/Scripts/python.exe -c "s=[-1.0/10,-2.0/1]; print('brief fixture correct?', s.index(max(s))==1); s=[-1.0/1,-2.0/10]; print('fixed fixture correct?', s.index(max(s))==1); from scripts.summarize_hellaswag import render_table; d={'n':1,'acc':0.5,'acc_norm':0.5,'ci95':[0,1],'retention_raw':1.0,'retention_floor_norm':1.0}; t=render_table({c:d for c in ('native','mapped-k1','rope-k1')}); f=[l for l in t.splitlines() if l.startswith('| ')]; print('filtered', len(f), 'rows[2:]', len(f[2:]), 'rows[1:]', len(f[1:]), 'conditions 3')"
