ts: 2026-08-24T03:37:17Z
commit: none (repo has zero commits at time of writing; `git log` errors with "does not have any commits yet")
session: Claude Code session d038135c-38c1-4c34-801a-b73544749faf, skeptic-main adversarial pass (flipped-split re-fit)
status: verified
fact: When a held-out score collapses while the in-sample score is high, the cheap alternative explanation is distribution shift between the train chunk and the held-out chunk rather than overfitting — and it is falsifiable by INVERTING which chunk is held out. Done here for the k=8 KV mapper: holding out the FIRST 10 of 50 calibration sequences instead of the last 10 gives K held-out R^2 = 0.0766 against 0.0984 on the original split (in-sample 0.9602 vs 0.9607, V held-out -0.7245 vs -0.6412). The collapse is unchanged, so the last-10 chunk is not unrepresentative and interpolation is the remaining explanation. Worth reusing as a standard cheap attack on any train/held-out gap claim: if flipping the split does not move the gap, distribution shift is refuted.
basis: skeptic-main refit k=8 in-process via `kvt.mapper.fit_mapper` (lam=0.01, reusing the original mapper's selected top-k layers so the split is the only variable), heldout = seqs 0-9, train = seqs 10-49, 10240 train tokens, runtime 535.5s. Reported "K_train=0.9602, K_heldout=0.0766, V_heldout=-0.7245" against the originals "K_train=0.9607, K_heldout=0.0984", the originals themselves recomputed bit-for-bit from `mappers/*.safetensors` + `data/kv/*` via `kvt.mapper.mapper_r2`, bypassing `results/mapper/*/r2.json`. Controller separately confirmed `kvt/ridge.py`'s `r2_score` measures against the mean of whichever set is scored, ruling out a train-mean artifact.
re-verify: .venv/Scripts/python.exe -c "import numpy as np; from kvt.data import KVDump; from kvt.mapper import Mapper, fit_mapper, mapper_r2; s=KVDump.load('data/kv/qwen3-0.6b-to-1.7b/source'); t=KVDump.load('data/kv/qwen3-0.6b-to-1.7b/target'); ho=s.seq_idx<10; m=fit_mapper(s,t,Mapper.load('mappers/qwen3-0.6b-to-1.7b/k8').selected,0.01,~ho); print(round(float(mapper_r2(m,s,t,ho)['K'].mean()),4))"
re-verify-caveat: EXPENSIVE (~9 min: it refits the k=8 mapper) and, unlike this ledger's other six re-verify lines, it was NOT executed end-to-end by the writer — the run was cut short deliberately. Treat the line as unconfirmed-as-executable; expect roughly 0.0766, and if it errors, the bug is in the line, not in the fact, whose basis is skeptic-main's completed 535.5s run.

